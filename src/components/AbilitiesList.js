// import React from 'react';
//
// const pickRandom = (arr) => arr[Math.floor(Math.random() * arr.length)];
// const padStats = (stats, value, separation, len) => {
//   value = value || 'xx';
//   return `${stats.toString()}${separation.repeat(len -
//       (value.toString().length + stats.toString().length))}${value.toString()}`;
// };
//
//
// export default AbilitiesList;
